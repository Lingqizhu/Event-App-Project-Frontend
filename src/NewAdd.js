import React, { useState } from "react";
import "./add.css"

function NewAdd(props) {
  const [disabled, cDisabled] = useState(false);

  const submitHandler = (e) => {

  };

  return (
    <div>
      Hello WorldHello World</div>
  );
}

export default NewAdd;
